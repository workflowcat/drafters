---
import { readingTimeLabel } from '~/lib/reading-time';
export interface Props {
  body: string;
  lang?: 'uk' | 'en';
}
const { body, lang = 'uk' } = Astro.props;
const label = readingTimeLabel(body, lang);
---
<span class="pill">{label}</span>
